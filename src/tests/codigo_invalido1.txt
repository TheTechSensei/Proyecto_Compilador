proto void sumar(int a, b); // Falta un tipo para el segundo parámetro

func int main() {
    int a = "Hola"; // Error: tipo incorrecto
    if (a === 5) {  // Error: operador no válido (=== no es válido en C)
        print "Error";
    }
    return 0;
}

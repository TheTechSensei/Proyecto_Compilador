proto int main // Falta el paréntesis para argumentos y punto y coma
func void calcular() {
    switch {
        case 1: // Falta un valor para comparar
            print "Error";
            break;
    }
    struct {
        int a, b // Falta el punto y coma al final de la declaración
    }
}
